// Backup Step 

/*var BotCollide = instance_place(x + 0, y + 1, [O_Collider]);
if ((BotCollide > 0))
{
	vspeed = 0;

	var SpacePressed;
SpacePressed = keyboard_check_pressed(vk_space);
if (SpacePressed)
{
	vspeed = -20;
}
}

else
{
	if(vspeed < GravityForce)
{
	vspeed += 1;
}

	else
{
	speed = GravityForce;
}
}

var DDown;
DDown = keyboard_check(ord("D"));
if (!DDown)
{
		var QDown;
	QDown = keyboard_check(ord("Q"));
	if (!QDown)
	{
		hspeed = 0;
		image_speed = 1;
	}
}

switch (Lives){
	case 0: draw_sprite(S_Life,3,x,y);
	case 1: draw_sprite(S_Life,2,x,y);
	case 2: draw_sprite(S_Life,1,x,y);
	case 3: draw_sprite(S_Life,0,x,y);
	
	break;
}*/